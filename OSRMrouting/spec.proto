syntax = "proto3";

option go_package = "github.com/JHU-Delivery-Robot/Server/OSRMrouting/spec";

package OSRMrouting;

service router {
  rpc route(Coords) returns (Route) {}
}

message Coords {
  int32 x = 1;
  int32 y = 2;
}

message Route {
  string path = 1;
}